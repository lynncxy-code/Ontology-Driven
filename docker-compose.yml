services:
  backend:
    build: ./backend
    ports:
      - "5000:5000"
    volumes:
      - ./backend:/app
      - ./frontend:/frontend
      - ./assets:/assets
      - ./exports:/exports
    environment:
      - ONTOTWIN_MIDDLEWARE_URL=http://host.docker.internal:5001
