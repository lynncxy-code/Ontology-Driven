version: '3.8'
services:
  backend:
    build: ./backend
    ports:
      - "5000:5000"
    volumes:
      - ./backend:/app
      - ./frontend:/frontend
    environment:
      - ONTOTWIN_MIDDLEWARE_URL=http://host.docker.internal:5001
