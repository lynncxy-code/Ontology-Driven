# OntoTwin 3.7 实施计划

> 文档性质：实施阶段拆分与原始工作量估算
> 功能设计来源：[OntoTwin 3.7 顶部信息面板垂直链路 (PRD)](../../PRD/OntoTwin%203.7%20顶部信息面板垂直链路%20%28PRD%29.md)
> 说明：本文件从 PRD 中迁出，不作为当前完成状态或验收结论。

## 1. 阶段拆分

### 阶段 A：存储和领域模型

- 增加 ProjectStore `schema_version` 和 v1 到 v2 迁移。
- 同步 JSON、PostgreSQL 读写和测试样例。
- 增加 `I3D_Overlay` 接口、模板 schema、继承和 revision 服务。

### 阶段 B：后端 API 与快照

- 新增 Overlay 独立模块和 API。
- 实现字段目录、校验、格式化、批量原子保存。
- 在快照中输出 `resolved_slots`。

### 阶段 C：场景交互工作台

- 新增 `/interaction` 页面和上下文入口。
- 实现类型配置、实例覆盖、同类型批量选择。
- 实现真实数据预览、继承提示和保存状态。

### 阶段 D：UE Widget 与交互

- 实现四个基础 UMG 模板。
- 实现 `selected` 与 `always` 两种渲染路径。
- 实现 bounds 锚点、离线状态、revision 更新和性能保护。

### 阶段 E：联调与回归

- 完成 JSON、PostgreSQL、Web、UE 垂直链路验收。
- 做 20、50、100 面板性能观察。
- 回归旧标签、Runtime Editor 和现有快照。

## 2. 原始工作量估算

| 阶段 | 原始估算 |
| --- | --- |
| A：存储和领域模型 | 2–3 人日 |
| B：后端 API 与快照 | 2–3 人日 |
| C：场景交互工作台 | 3–4 人日 |
| D：UE Widget 与交互 | 4–6 人日 |
| E：联调与回归 | 2–3 人日 |

单人顺序开发原始估算为 13–19 人日，不包含视觉稿反复调整、视频能力和大规模标签聚合。
