# OntoTwin 3.0.1 — 实例绑定台需求说明书 (PRD)

> 版本：3.0.1
> 依赖：《OntoTwin 3.0 本体与 CAD 分层架构重构总纲》
> 适用：单人开发，不过度工程化。前端走 CDN，UI 遵循 `ontotwin-ui` 规范。

---

## 1. 背景

CAD 解析出的是**匿名构件**：有类型、几何、坐标，但没有业务身份证号。要让构件成为可监控、可被 UE 渲染的**实例**，必须把它和一份**业务实例清单**里的身份证号绑定起来。

本页就是做这件事的地方——一个**对账/撮合视图**：左边是 CAD 构件，右边是业务身份，把身份证号"焊"上去，铸造出实例。

---

## 2. 目标

1. 让用户把当前项目的 CAD 构件，逐个（或批量）绑定到业务身份证号。
2. 身份来源支持**上传清单**与**手动录入**两种。
3. 尽量自动撮合（利用 CAD 块自带的 `EQUIP_ID`），把人工工作量降到最低。
4. 绑定完成即铸造为实例，进入实例运维 / UE。

**非目标**：不做 MES 实时对接、不做多人协作、不做绑定历史版本管理。

---

## 3. 名词

| 词 | 含义 |
|---|---|
| 构件（Component） | CAD 标定产出的匿名实物（类型 + 几何 + 坐标） |
| 身份证号（Instance ID） | 业务系统里设备的唯一编号，如 `DW-007` |
| 实例清单（Roster） | 一批身份证号的目录（上传或手填） |
| 绑定（Binding） | 构件 ↔ 身份证号 的 1:1 关联 |
| 铸造（Mint） | 绑定成立后，由构件生成正式实例 |
| 悬空身份 | 清单里有、但没有任何构件与之匹配的身份证号 |

---

## 4. 页面位置与入口

- 分组：**接口治理**（与映射控制台同级）。
- 路由：`/binding`（建议），页面 `frontend/binding.html`。
- 作用域：只针对**当前激活项目**的构件与清单（沿用唯一可见性规则）。
- 前置条件：当前项目已在坐标标定工作台产出构件；否则显示空态引导"请先在坐标标定工作台标定并保存构件"。

---

## 5. 功能详述

### 5.1 整体布局（对账双栏）

```
┌─────────────────────────────┬──────────────────────────────┐
│  左：CAD 构件（当前项目）      │  右：业务实例清单（身份证号）    │
│  - 类型 / 名称 / 坐标 / EQUIP_ID │  - 上传 CSV / 手动录入          │
│  - 绑定状态徽标               │  - 身份证号 / 类型 / 名称 / 区域 │
├─────────────────────────────┴──────────────────────────────┤
│  顶部工具条：自动撮合 | 仅看未绑定 | 搜索 | 铸造为实例（N）       │
└──────────────────────────────────────────────────────────────┘
```

### 5.2 右侧实例清单来源（两种）

**（a）上传 CSV / Excel**
- 期望列（表头中文/英文兼容，缺列容错）：
  `身份证号(必填) | 类型 | 名称 | 区域/位置提示(可选) | EQUIP_ID(可选,用于精确撮合)`
- 解析后进入 `instance_roster`，逐行显示。

**（b）手动录入**
- "+ 新增一行"，手填身份证号（+ 可选类型/名称）。
- 用于没有 MES 导出、或临时补录的场景（决策 Q2：允许清单外手填）。

### 5.3 撮合逻辑（规则自动 + 人工兜底）

按优先级自动撮合，命中即建议绑定（用户可一键采纳/逐条确认）：

1. **EQUIP_ID 精确匹配**：构件 `attribs.EQUIP_ID` == 清单某行的身份证号（或其 EQUIP_ID 列）→ 强匹配，自动绑。
2. **类型 + 区域 + 顺序就近**：同类型、同区域内，按坐标顺序与清单顺序对位分配（弱匹配，需人工确认）。
3. **剩余**：标"未绑定"，等人工处理。

人工操作：
- **手选**：点一个构件 → 从清单下拉选一个身份证号绑定（1:1，已被占用的号置灰）。
- **手填新号**：构件可直接填一个清单外的身份证号成实例（决策 Q2）。
- **解绑**：撤销某条绑定。

### 5.4 状态与可见性

- 构件三态徽标（用 `ontotwin-ui` 的 6px 状态点，禁 emoji）：
  - `已绑定`（dot-ok）：已关联身份证号。
  - `未绑定`（dot-warn）：尚无身份。
  - `纯几何`（dot-muted）：墙/地面等无需身份的构件，可标记为"不参与绑定"。
- 清单侧标注：**悬空身份**（dot-err）= 有号无构件，提示"无几何 / 待定位"。
- 工具条"仅看未绑定"快速过滤。

### 5.5 铸造为实例

- 顶部"铸造为实例（N）"：把所有已绑定构件铸造/更新为实例，写入 `ProjectStore.instances`，身份证号即实例主键。
- 铸造后：实例进入实例运维 / 监控，UE 快照可见（决策 Q3）。
- 可逆：解绑 + 重铸造允许；遵循 `ontotwin-ui` 可逆性约定，成功后保留编辑区与回退入口。

---

## 6. 数据结构

```jsonc
// 构件（坐标标定写入，绑定台读 + 回写 bound_instance_id）
component = {
  "id": "cmp_xxx",
  "object_type_rid": "type_rongtongcao",
  "type_name": "溶铜槽",
  "cad_xy": [35008, 968861],
  "ue_xy": [16.13, 29.88],
  "attribs": { "EQUIP_ID": "DW-007" },   // 可能为空 → 这正是要解决的"无身份证号"
  "asset_path": "/Game/.../DW",
  "bound_instance_id": "DW-007"           // 绑定后回填；未绑定为 null
}

// 实例清单行
roster_entry = {
  "instance_id": "DW-007",   // 身份证号，主键
  "type": "溶铜槽",
  "name": "溶铜槽 7 号",
  "area": "5F-A区",
  "equip_id": "DW-007",      // 可选，撮合用
  "source": "upload" | "manual"
}

// 实例（铸造产物，写入 ProjectStore.instances，键=身份证号）
instance = {
  "id": "DW-007",
  "component_id": "cmp_xxx",
  "object_type_rid": "type_rongtongcao",
  "raw_state": { ... },       // 沿用默认 raw_state
  "created_at", "last_seen", "status"
}
```

> 涉及 ProjectStore JSON 结构新增 `components` / `instance_roster` 字段——开发前按 CLAUDE.md 向用户确认。

---

## 7. API 草案（以当前激活项目为作用域）

| 方法 | 路径 | 作用 |
|---|---|---|
| GET | `/api/v2/binding/components` | 列当前项目的构件（含绑定状态） |
| GET | `/api/v2/binding/roster` | 取实例清单 |
| POST | `/api/v2/binding/roster/upload` | 上传 CSV/Excel 解析入清单 |
| POST | `/api/v2/binding/roster` | 手动新增/编辑清单行 |
| POST | `/api/v2/binding/automatch` | 触发自动撮合，返回建议绑定列表 |
| POST | `/api/v2/binding/bind` | 绑定单条（component_id ↔ instance_id），含 1:1 占用校验 |
| POST | `/api/v2/binding/unbind` | 解绑 |
| POST | `/api/v2/binding/mint` | 把已绑定构件铸造/更新为实例 |

---

## 8. UI 规范要点（遵循 `ontotwin-ui`）

- 极简黑白灰；状态用 6px 点 + 文字，禁 emoji。
- 上传/铸造等写操作：toast 反馈，危险操作（解绑全部/覆盖）走模态二次确认。
- 一个区域只有一个 primary 黑底按钮（撮合页主操作=铸造；上传/自动撮合为次级）。
- 文案走翻译表：不出现 `rid` / `EQUIP_ID` 等黑话给最终用户，统一说"类型/设备编号/身份证号"。
- 空态：无构件时引导去坐标标定；无清单时引导上传或手填。

---

## 9. 验收清单

- [ ] 坐标标定产出的构件能在本页左栏列出，含类型/坐标/EQUIP_ID/绑定状态。
- [ ] 上传 CSV 能正确解析入清单；缺列容错不崩。
- [ ] 手动录入身份证号可用。
- [ ] 自动撮合：EQUIP_ID 精确匹配能一键绑定一批。
- [ ] 手选 / 手填新号 / 解绑均可用，1:1 占用校验生效。
- [ ] 铸造后实例出现在实例运维，且身份证号正确；UE 快照可见。
- [ ] 未绑定构件不进实例运维、不进 UE（决策 Q3）。
- [ ] 全程遵循 `ontotwin-ui`（无 emoji、无原生 alert、可逆）。

---

## 10. 开放问题

- 同名冲突：上传清单的身份证号与项目内已存实例重号时——跳过 / 覆盖 / 提示？（建议提示二选一）
- 区域字段从哪来：CAD 图层名能否直接当"区域"用于就近撮合？（待确认图层语义）
- 纯几何构件是否需要在本页显式"标记为不参与绑定"，还是按类型自动判定（墙/地面）。

---

*文档版本 v1 · 待确认后进入开发。*
