# OntoTwin 3.0.1.1 — 实例绑定台自动编号补丁（PRD）

> 版本：3.0.1.1  
> 父模块：3.0.1 实例绑定台  
> 状态：已确认，进入开发  
> 日期：2026-08-19

---

## 1. 编号结论

实例绑定台的正式模块编号是 **3.0.1**。依据如下：

- 《OntoTwin 3.0 本体与 CAD 分层架构重构总纲》明确把实例绑定台定义为 3.0.1。
- 现有详细需求文档名为《OntoTwin 3.0.1 实例绑定台需求说明书》。
- 后端绑定 API 代码区也标记为“3.0.1 实例绑定台 API”。

本需求是对既有实例绑定台的增量补丁；`3.0.2` 已用于图片模式分层架构，因此本次编号定为 **3.0.1.1**。

---

## 2. 背景与问题

现有绑定台要求用户先准备实例清单，再从清单中选择实例编号与构件绑定。真实操作中经常只有已经标定好的构件，没有 MES、台账或其他业务清单，用户只能逐条手填编号，无法快速铸造实例。

当前数据链路已经允许系统补齐这一环：

```text
匿名构件 → 自动生成实例编号 → 写入本项目实例清单 → 1:1 绑定 → 用户确认铸造
```

自动编号不是绕过实例清单，而是由系统代替用户生成一份项目内清单，继续保持“构件、身份、实例”三层关系。

---

## 3. 目标与非目标

### 3.1 目标

1. 没有实例清单时，用户可以通过筛选和勾选，为指定的一部分或全部未绑定构件生成可用编号。
2. 自动编号、写入清单、建立绑定必须一次成功或全部不发生。
3. 生成前提供编号规则和结果预览，生成后仍可逐条解绑或调整。
4. 复用现有铸造入口，不改变实例和 UE 同步的数据契约。
5. 不修改 ProjectStore 文件结构，不新增依赖或前端构建步骤。

### 3.2 非目标

- 不建设企业级编码规则中心。
- 不从设备类型自动推导企业缩写。
- 不自动重命名已经绑定或已经铸造的实例。
- 第一版不把“自动编号”和“铸造”合并为无确认的一键写入。

---

## 4. 已确认产品决策

| 项目 | 决策 |
|---|---|
| 编号作用域 | 当前激活项目 |
| 默认格式 | `AUTO-0001` |
| 默认前缀 | `AUTO`，用户可修改 |
| 默认位数 | 4 位 |
| 默认起点 | 同前缀已占用最大序号 + 1；没有占用时从 1 开始 |
| 处理范围 | 用户显式勾选的未绑定构件；已绑定构件不可选、不会重新编号 |
| 写入结果 | 新增清单行 `source: "auto"`，并回填构件 `bound_instance_id` |
| 铸造行为 | 编号绑定成功后仍由用户点击现有“铸造为实例” |
| 可逆性 | 保留解绑、手选其他编号和再次操作入口 |

---

## 5. 编号规则

### 5.1 格式

```text
<前缀>-<补零序号>
```

示例：

- `AUTO-0001`
- `PUMP-0001`
- `AHU-A-0001`

前缀仅允许英文字母、数字、短横线和下划线，长度 1–24；补零位数允许 2–8，默认 4。前缀统一转为大写，避免 `auto-0001` 与 `AUTO-0001` 形成肉眼难辨的两套编号。

### 5.2 占用集合

生成前必须同时扫描：

1. `instance_roster` 中已有编号；
2. `instances` 中已经铸造的实例编号；
3. `components[].bound_instance_id` 中已有绑定；
4. 本批次已经分配的候选编号。

新编号不得与以上任一集合冲突。序号只向后追加，不主动填补历史空洞。

### 5.3 构件顺序

为保证相同项目多次预览结果稳定，已选构件按以下键排序：

1. 来源名称 `source_label`；
2. 楼层 `floor`；
3. 类型名称 `type_name`；
4. 来源坐标 Y、X；
5. 构件稳定 ID。

坐标缺失时按 0 处理，构件 ID 作为最终稳定兜底。

---

## 6. 页面交互

### 6.1 入口

- 编号入口放在左侧“构件”面板的范围操作栏中，按钮文案为“自动编号”。
- 实例清单为空时，空态说明“先在左侧筛选并勾选构件，再自动编号”。
- 没有勾选构件时“自动编号”按钮禁用，并显示“未选择构件”。

### 6.2 范围筛选与选择

- 搜索框和“只看未绑定”组成独立筛选行，只负责缩小左侧可见范围。
- 每个未绑定构件左侧增加复选框；已绑定构件不提供复选框。
- 使用固定文案的“全选筛选结果”复选框控制当前筛选后的未绑定构件；部分选中时显示半选状态。
- “清除”清除所有选择，包括当前被筛选隐藏的选择。
- 选择跨筛选条件保留；范围操作栏显示“已选择 N 个构件”，存在隐藏选择时补充“其中 M 个不在当前结果”。
- 自动编号始终只提交已选构件 ID；不得把“当前可见”或“全部未绑定”作为隐式提交范围。

### 6.3 预览模态

模态包含：

- 已选待编号构件数量；
- 前缀输入框；
- 序号位数；
- 服务端返回的实际起始编号、结束编号；
- 最多 5 条“构件类型 → 新编号”预览；
- 常驻说明：“生成后将加入本项目实例清单并完成绑定，不会立即铸造。”

用户修改前缀或位数后重新预览。主操作为“生成并绑定”，取消为次级操作。异步期间按钮禁用并显示“生成中…”。

### 6.4 完成态

成功后：

- toast 提示“已生成并绑定 N 个实例编号”；
- 左侧构件立即显示新编号；
- 右侧清单显示自动生成的编号；
- 现有“铸造为实例（N）”继续作为唯一主操作。

同时修正现有铸造说明：后端会保留既有自由实例，因此不得再写“实例集 = 已绑定构件集”。

---

## 7. 后端接口

新增：

```http
POST /api/v2/binding/auto-number
Content-Type: application/json
```

请求：

```json
{
  "prefix": "AUTO",
  "width": 4,
  "component_ids": ["cmp_a", "cmp_b"],
  "dry_run": true,
  "expected_project_id": "project_id"
}
```

- `component_ids` 为页面显式选择的构件 ID，API 请求中必填；缺省时返回 400，不允许隐式处理全部构件。
- `dry_run=true` 只返回预览，不写入。
- `dry_run=false` 在同一项目锁内完成清单写入与构件绑定。
- 激活项目与 `expected_project_id` 不一致时返回 `409 project changed`，且零写入。

响应：

```json
{
  "status": "preview",
  "project_id": "project_id",
  "count": 2,
  "prefix": "AUTO",
  "width": 4,
  "start": 1,
  "end": 2,
  "pairs": [
    {"component_id": "cmp_a", "instance_id": "AUTO-0001", "type_name": "风机"},
    {"component_id": "cmp_b", "instance_id": "AUTO-0002", "type_name": "水泵"}
  ]
}
```

---

## 8. 原子性与错误处理

自动编号不能由前端依次调用“新增清单”和“批量绑定”，否则第二步失败会留下没有构件的清单编号。

后端必须在单个 `ProjectStore` 事务中：

1. 校验项目未切换；
2. 校验目标构件存在且仍未绑定；
3. 计算未占用编号；
4. 写入自动清单行；
5. 回填全部构件绑定；
6. 只保存一次项目。

任一步失败均不改变当前项目。预览后若构件状态已变化，提交应返回错误并要求刷新，不静默覆盖现有绑定。

---

## 9. 数据兼容性

现有清单行已经包含 `source` 字段，本补丁只新增取值 `auto`：

```json
{
  "instance_id": "AUTO-0001",
  "type": "风机",
  "name": "风机",
  "source": "auto"
}
```

不增加 ProjectStore 顶层字段，不提升 `schema_version`，JSON 与 PostgreSQL 两种存储后端继续复用同一业务逻辑。

---

## 10. 验收标准

- [ ] 未选择构件时自动编号按钮禁用，不能隐式处理全部构件。
- [ ] 搜索和“仅看未绑定”可以缩小可见范围。
- [ ] 支持逐条勾选、全选/取消当前筛选结果、清空全部选择。
- [ ] 选择跨筛选保留，范围操作栏显示选择总数和不在当前结果的数量。
- [ ] 自动编号预览和提交只包含用户已选构件。
- [ ] 默认生成 `AUTO-0001` 开始的连续编号。
- [ ] 已存在 `AUTO-0001`、`AUTO-0003` 时从 `AUTO-0004` 继续，不回填 `AUTO-0002`。
- [ ] 清单、实例或构件绑定中任一重号都不会被再次分配。
- [ ] 预览不修改项目文件、清单或构件。
- [ ] 确认后清单写入与绑定一次完成，且只保存一次。
- [ ] 已绑定构件不会被重新编号。
- [ ] 项目切换时返回 409，错误项目零写入。
- [ ] 自动编号后仍可解绑和手动改绑。
- [ ] 自动编号不会自动铸造；用户确认铸造后实例与 UE 沿用现有链路。
- [ ] 页面无 emoji、无原生 `alert/confirm/prompt`，加载态、空态、焦点回退符合 `ontotwin-ui`。

---

## 11. 实施范围

| 文件 | 改动 |
|---|---|
| `backend/project_store.py` | 自动编号规划与原子写入 |
| `backend/app.py` | 自动编号 API、绑定台项目 ID 返回 |
| `frontend/binding.html` | 自动编号入口、预览模态、结果反馈 |
| `backend/tests/test_binding_auto_number.py` | 规划、冲突、原子性、API 测试 |

不修改现有路由，不修改 ProjectStore 文件结构，不引入新依赖。
