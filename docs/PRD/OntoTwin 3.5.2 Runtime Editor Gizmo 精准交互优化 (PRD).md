# OntoTwin 3.5.2 Runtime Editor 编辑模式优化 (PRD)

> 状态：已实现，待 PIE 交互验收  
> 主线：OntoTwin Nexus / UE Runtime / OntoTwinSync 通用插件  
> 日期：2026-07-30  
> 目标版本：3.5.2  
> 前置版本：3.5.1 Runtime Editor 交互优化

---

## 1. 背景

3.5.1 已在打包运行时提供自由相机、单实例选择、XY/Z/Yaw Gizmo、网格与墙面吸附、保存和取消。3.5.2 在保留 Runtime-only、插件自包含和 OntoTwin desired state 回写语义的基础上，补齐日常场景维护需要的批量编辑能力：

- `Shift + 单击`多选和临时成组变换。
- 从场景移除，但不删除实例数据。
- 会话级暂存、撤销、重做和统一保存。
- 原子批量回写与并发冲突检测。
- 完整简体中文面板和明确的离开确认。
- Bounds Center Gizmo、高辨识度轴向和角标式选中反馈。

本能力属于 `OntoTwinSync` 通用插件，不与 `test0316`、`ZHHZ` 或特定关卡绑定。

---

## 2. 产品边界

### 2.1 目标

用户可以在 F8 Runtime Editor 中连续选择 OntoTwin 已接管的实例，完成单个或批量平移、Z 轴移动、Yaw 旋转、吸附和从场景移除，并将整个编辑会话一次性保存为 OntoTwin desired state。

### 2.2 非目标

- 不删除实例、绑定、模型覆盖或业务属性。
- 不保存 `.umap`，不调用 `GEditor`、`UnrealEd` 或其他 Editor-only API。
- 不编辑未由 `ATwinSceneManager` 管理的历史 Actor。
- 不做框选、多选缩放、复制、任意轴旋转、完整避障或禁放区。
- 不在本版本新增 actual state 上报；实时位置源仍由现有同步链路负责。
- 不增加中英文切换和运行时可拖拽面板布局。

---

## 3. 已确认交互

### 3.1 选择与临时成组

- 普通左键单击：用命中实例替换当前选择。
- `Shift + 左键`：追加或取消单个实例。
- 单击空白处：清空当前选择，但保留会话内已暂存修改。
- 本期不支持框选，避免空白拖动误触和遮挡选择歧义。
- 单次选择最多 20 个实例；整个 F8 会话最多累计 100 个待保存实例。
- 超限时保持原选择和暂存区不变，并显示中文 Toast。
- 多选后形成临时组；所有成员地位一致，不设置主对象。

### 3.2 组合 Gizmo

- 单选 Gizmo 位于实例可见模型 Bounds Center。
- 多选 Gizmo 位于所有成员组合 Bounds Center。
- 始终使用世界坐标轴：X 红、Y 绿、Z 蓝，Yaw 环橙色。
- 平移对所有成员应用相同世界位移，保持相对间距。
- Yaw 旋转使所有成员绕组合 Bounds Center 旋转，同时更新各自 Yaw。
- 网格吸附作用于组合中心，再向所有成员应用同一位移。
- 墙面吸附按组合 Bounds 判断，整组靠墙并保持相对布局。
- 单选面板显示绝对 `X/Y/Z/Yaw`；多选显示组合中心 `X/Y/Z` 和组旋转增量 `Delta Yaw`。

### 3.3 选中反馈

- 每个被选实例绘制短白色 Bounds 角标，不绘制完整边线和深色描边。
- 多选额外绘制一套蓝色组合 Bounds 角标。
- 组合 Gizmo 位于蓝色角标范围中心。
- 数量较多时降低单体角标亮度，避免视觉噪声。
- Gizmo 和 Bounds 不投射阴影，不依赖调试绘制 API，在 Shipping 中保持一致。

### 3.4 从场景移除

- 操作名称固定为“从场景移除”，不使用“删除”。
- 面板按钮和 `Delete` 快捷键均可触发。
- 点击后对象立即在本地隐藏，并从当前选择中移除。
- 保存前只写入会话暂存区，可用撤销或取消全部恢复。
- 保存成功后写入 `raw_state.is_loaded=false`，不删除实例及其其他数据。
- Web 实例页“加载进场景”写回 `is_loaded=true` 后可恢复显示。
- `I3D_Visual.is_visible` 不承担本操作语义。

### 3.5 撤销与重做

- `Ctrl + Z` 撤销，`Ctrl + Y` 重做。
- 一次 Gizmo 按下、拖动、松开记为一步，不记录拖动中间帧。
- 一次临时组平移、旋转或从场景移除均记为一步。
- 选择、相机、吸附开关不进入操作历史。
- 最多保留 50 步；执行新操作后清空重做栈。
- 保存成功或取消全部后清空撤销、重做栈。

### 3.6 Esc 与退出

`Esc` 按上下文处理：

1. 正在拖动 Gizmo：恢复到按下前，不产生操作记录。
2. 已打开确认窗口：关闭窗口并继续编辑。
3. 普通编辑状态：清空当前选择，不清空暂存区。

再次按 `F8`：

- 无待保存内容时直接退出。
- 有待保存内容时显示“保存并退出 / 放弃修改并退出 / 继续编辑”。
- 保存失败时不退出并保留本地编辑现场。

---

## 4. 会话状态模型

Runtime Editor 从单实例 dirty 状态升级为会话级暂存区：

```text
当前选择集 SelectedInstances
待保存状态 StagedStates[instance_id]
撤销栈 UndoStack[最多 50]
重做栈 RedoStack[最多 50]
```

每个会话实例状态至少包含：

```text
instance_id
display_name
baseline_transform
current_transform
baseline_is_loaded
current_is_loaded
baseline_state_hash
local_lock / animation_state
change_flags: transform | removed
```

规则：

- 选中实例立即启用本地同步锁，并暂停可能影响位置的运行时动画。
- 取消选择且未修改的实例立即解锁并恢复动画。
- 已修改或待移除实例即使不再选中，也保持锁定直到保存、取消全部或退出。
- 未选择、未修改的其他实例继续轮询和运行。
- 保存后保留当前选择和 Gizmo，保存结果成为新基线。
- “取消全部修改”恢复整个暂存区并清空历史；执行前需要二次确认。

---

## 5. 数据与 API

### 5.1 原子批量回写

新增批量接口，保留原单实例 `/api/v2/state/writeback`：

```http
POST /api/v2/state/writeback/batch
```

请求示意：

```json
{
  "changes": [
    {
      "instance_id": "robot_001",
      "expected_state_hash": "sha256:...",
      "transform": {
        "tx": 100.0,
        "ty": 200.0,
        "tz": 0.0,
        "rx": 0.0,
        "ry": 0.0,
        "rz": 90.0,
        "sx": 1.0,
        "sy": 1.0,
        "sz": 1.0
      },
      "is_loaded": true
    }
  ]
}
```

- 后端先校验工程绑定、条数、实例存在性、Transform、`is_loaded` 和全部状态摘要。
- 任意一项失败或冲突时整批不写入。
- 状态摘要只覆盖 Runtime Editor 可写字段，不使用全局快照 revision，避免无关实例更新造成冲突。
- 冲突返回 `409` 和冲突实例 ID；F8 标记冲突项，不提供强制覆盖。
- 文件存储先在副本上应用并一次持久化；PostgreSQL 存储使用事务语义。
- 不修改已有项目文件结构。

### 5.2 空间编辑许可

实例增加可选字段：

```json
{"runtime_spatial_editable": true}
```

- 缺失时默认为 `true`，兼容所有现有数据。
- Web 实例页提供“允许运行时空间编辑”开关。
- 为 `false` 时 UE 继续展示和同步实例，但 F8 不允许选中，并显示“该实例由实时位置源驱动”。
- 该字段由快照下发，属于 OntoTwin 数据能力，不依赖 UE 关卡预绑定。

### 5.3 并发冲突

- 本地 `bLocalOverrideLock` 不视为后端锁。
- 选中时缓存可写状态摘要，保存时采用乐观并发校验。
- 发生冲突时整批拒绝、保留本地暂存和锁定状态。
- 用户可以“放弃本地修改并刷新”，本期不提供强制覆盖。

---

## 6. 中文面板

### 6.1 布局

- 左上角固定，宽约 440 px。
- 内容区最大高度约为视口 70%，超出后内部滚动。
- 底部操作栏固定，不因列表增长离开可视区域。
- 标题栏：撤销、重做、关闭。
- 实例区：实例名称、实例 ID、当前选择数量、Transform 和“从场景移除”。
- 吸附区：墙面吸附、网格吸附。
- 待保存区：默认折叠，显示计数；保存失败时自动展开并标记失败项。
- 底部：“取消全部修改”“保存”。

### 6.2 文案

- 所有标题、按钮、状态、Tooltip、Toast 和确认窗口使用简体中文。
- 实例名称和实例 ID 的字段标题使用中文，值保持原始数据。
- 名称缺失时回退到实例 ID。
- `X/Y/Z/Yaw` 保留行业通用写法并标明单位。
- 用户界面显示友好错误；HTTP 状态码和原始响应进入日志。

---

## 7. 输入优先级

```text
确认窗口 > Gizmo 拖动 > 面板交互 > 快捷键 > 场景选择 > Runtime Editor 相机
```

- `Shift + 左键`只在命中实例时切换多选。
- 命中 Gizmo 时优先开始拖动，不改变选择。
- `Delete`只在 F8、存在选择、未拖动、未保存且文本输入未聚焦时生效。
- `Ctrl + S/Z/Y`只在 F8 生效。
- 右键拖动继续控制相机；WASD、Q/E 和滚轮不被面板悬浮状态误触发。

---

## 8. 验收标准

### 8.1 功能

- `Shift + 单击`可以稳定选择、取消选择最多 20 个实例。
- 多选 Gizmo 位于组合 Bounds Center，世界轴平移保持相对间距。
- 组合 Yaw 绕组中心旋转，不围绕任一 Actor Pivot。
- 网格和墙面吸附对整组生效，不打散队形。
- 单体白色角标、整组蓝色角标和高饱和 Gizmo 在复杂背景中清晰可见。
- 从场景移除立即隐藏；撤销恢复；保存后 Web“加载进场景”可以恢复。
- 会话可跨选择累计最多 100 个待保存实例。
- 撤销、重做、取消全部、保存并退出和放弃退出符合本 PRD。
- 保存成功后保留选择，清空历史并推进基线。
- 并发状态变化返回冲突，不发生部分保存或静默覆盖。
- `runtime_spatial_editable=false` 的实例不可在 F8 选中。

### 8.2 数据

- 批量请求全部成功或全部失败。
- 绑定 CAD 构件的实例继续正确回写 canonical 坐标。
- 自由实例继续写入 `raw_state`。
- “从场景移除”只修改 `is_loaded`，不删除实例和其他字段。
- 现有单实例 writeback 保持兼容。

### 8.3 构建与环境

- 后端相关自动测试通过。
- `test0316Editor` PIE 验收通过。
- `ZHHZEditor` PIE 验收通过。
- OntoTwinSync 在目标 UE 5.6 环境完成 Development/Shipping 编译。
- 本轮不要求生成完整 RC 安装包。

---

## 9. 实施顺序

1. 后端增加批量服务、状态摘要、原子提交和测试。
2. 快照和 Web 实例页增加空间编辑许可字段。
3. UE 将单实例状态重构为选择集与会话暂存区。
4. 实现临时组 Transform、组合 Bounds、吸附和撤销栈。
5. 实现从场景移除、退出确认和冲突反馈。
6. 重构并汉化 UMG 面板。
7. 完成双项目 PIE 与 Shipping 编译验收。

---

## 10. 实施记录（2026-07-30）

已完成：

- 后端新增 `POST /api/v2/state/writeback/batch`，支持最多 100 个实例的原子保存。
- 快照新增 `runtimeEditor.spatialEditable` 与 `runtimeEditor.stateHash`。
- Web 实例页新增“允许空间编辑 / 实时位置只读”，并统一“加载进场景”的 `is_loaded` 语义。
- UE Runtime Editor 支持 `Shift + 单击`多选、临时组移动/旋转、组合吸附、从场景移除、撤销/重做和会话级暂存。
- 中文面板完成 440 px 布局、70% 视口高度约束、内部滚动、固定底栏、待保存清单和退出/取消确认。
- Gizmo 使用组合 Bounds Center；单体白色角标与多选蓝色组合角标均为 Shipping 可用的运行时组件。
- 后端批量写回单元测试 4 项通过。
- `test0316Editor Win64 Development` 编译通过。
- `test0316 Win64 Shipping` 编译通过，确认未依赖 Editor-only API。
- 运行中的后端快照已验证返回空间编辑许可和状态摘要。

待人工 PIE 验收：

- 在 `test0316` 中完成多选、成组变换、吸附、移除、撤销、保存和退出确认的视觉与手感验收。
- ZHHZ 工程持有独立且已有差异的插件副本；同步前需明确保留策略，避免直接覆盖项目侧在途修改。
