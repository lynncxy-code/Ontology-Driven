## PHASE1_GENERATION_PLAYBACK — Phase 1 主线生成回放验证\n\n> **目的**：用一组接近真实需求的任务描述，走完 `Scene → Task → Type/Mode → Template → Level → 审计` 主链，验证当前主线在“实际生成页面”这一层是否也体现出稳定性与结构分化能力，而不是继续过度收敛到少数模板。\n\n---\n\n## 1. b_system 场景生成回放\n\n### 1.1 普通工单列表（list + 顶部筛选条）\n- **任务描述**：\n  - “航空制造后台的工单列表页，需要按状态/优先级/负责人筛选工单，支持搜索工单号和批次号，表格中展示工单基础信息与当前状态。”\n- **预期路径**：\n  - scene = `b_system`\n  - 预期 task_id ≈ `b_system_list`\n  - page_type / target_type = `list`\n  - level = `level_1`\n  - template ≈ `examples/b-system/b-system-task-list-top-filters.html`\n- **生成回放（以样例模板为代表）**：\n  - 实际壳：`b-layout-sidebar + b-sidebar + b-main + b-header`，主内容为 `content-card + search-bar + data-table-container`，筛选条件全部集中在表格上方的顶部筛选条。\n  - 与预期一致：未引入左侧 `filter-panel`，结构明显属于“普通列表 + 顶部筛选”。\n- **审计结果**：\n  - `skill-audit.js ... --scene b_system` → `✓ PASS`，`ERROR: 0 / WARN: 0 / exit=0`。\n  - 说明在当前 class 与壳约束下，list 结构可以稳定通过审计。\n\n### 1.2 高级筛选列表（advanced_list + 左侧筛选面板）\n- **任务描述**：\n  - “质检工单的高级列表页，需要在左侧长期展示多维筛选条件（产品线/工艺段/质检结论/缺陷类型等），右侧为可以横向滚动的大表格，支持根据筛选实时刷新。”\n- **预期路径**：\n  - scene = `b_system`\n  - task_id = `b_system_advanced_list`\n  - target_type = `advanced_list`\n  - level = `level_2`（相对普通 list 更复杂）\n  - template ≈ `examples/b-system/b-system-advanced-list-with-left-filter-panel.html`\n- **生成回放（以样例模板为代表）**：\n  - 实际壳：在 `b-layout-sidebar` 下，主区内部再分为左侧 `filter-panel` + 右侧 `advanced-data-table`，左侧承载多维筛选 tags/checkbox，右侧表格保持高信息密度。\n  - 与预期一致：结构清晰体现“左侧常驻筛选面板 + 右侧主表格”，与 1.1 的顶部筛选 list 有明显区分。\n- **审计结果**：\n  - `skill-audit.js ... --scene b_system --task b_system_advanced_list` → `✓ PASS`，`ERROR: 0 / WARN: 0 / exit=0`。\n  - 高级列表 Guard 认可该结构为合法 advanced_list 壳。\n\n### 1.3 工单详情页（detail）\n- **任务描述**：\n  - “单个生产工单详情页，包含工单基础信息、状态时间线、质检记录与关联工序记录，需要支持在详情里看到完整流转过程。”\n- **预期路径**：\n  - scene = `b_system`\n  - task_id = `b_system_detail`\n  - target_type = `detail`\n  - level = `level_1`\n  - template ≈ `examples/b-system/b-system-detail-order.html`\n- **生成回放（以样例模板为代表）**：\n  - 实际壳：主内容围绕单个工单卡片 + 时间线 + 若干关联列表展开，没有在顶部堆叠 `stats-grid` / `charts-grid` 的仪表盘壳。\n  - 与预期一致：页面重心是“看一条工单的完整信息”，不是整体运营总览。\n- **审计结果**：\n  - `skill-audit.js ... --scene b_system --task b_system_detail` → `✓ PASS`，`ERROR: 0 / WARN: 0 / exit=0`。\n  - `b_system_detail` 的“误升级为仪表盘壳” Guard 未被触发。\n\n### 1.4 生产运营总览（dashboard，作为模糊边界对照）\n- **任务描述**：\n  - “生产运营总览页，需要在一个页里看到产线任务 KPI、工单分布、异常动态、重点待办，图表为主，列表为辅。”\n- **预期路径**：\n  - scene = `b_system`\n  - task_id ≈ `b_system_dashboard_overview`\n  - target_type = `dashboard`\n  - level = `level_1`\n  - template ≈ `examples/b-system/b-system-complete.html`\n- **生成回放（以样例模板为代表）**：\n  - 实际壳：在 `b-layout-sidebar` 下，首页主区以 `stats-grid + charts-grid + content-card 列表` 组成典型仪表盘；列表只作为“重点工单/待办”卡片出现，不是主视图。\n  - 与 1.1/1.2/1.3 相比，结构显著偏向 KPI/图表总览，而不是“查列表”或“看详情”。\n- **审计结果**：\n  - `skill-audit.js ... --scene b_system --task b_system_dashboard_overview` → `✓ PASS`。\n  - 说明当前仪表盘样例在 Guard 与真值源下被视为合法 dashboard 壳。\n\n> **b_system 小结**：在这 4 条任务下，实际代表生成的结构分别落在 `b-system-task-list-top-filters` / `b-system-advanced-list-with-left-filter-panel` / `b-system-detail-order` / `b-system-complete` 四个不同模板族上，审计全 PASS，说明主线已经能在生成层面明显区分 list / advanced_list / detail / dashboard，而不是全部压回单一模板。\n\n---\n\n## 2. ue5_overlay 场景生成回放\n\n### 2.1 Mode 1 — 单 HUD 监控大屏\n- **任务描述**：\n  - “设备运行监控视图，需要顶部少量运行 KPI，主视图是三维场景和 world-marker，偶发告警可以在 HUD 上快速看到，不需要完整告警中心。”\n- **预期路径**：\n  - scene = `ue5_overlay`\n  - task_id = `ue5_overlay_mode_1_single_hud`\n  - layout_mode / target_mode = `1`\n  - page_type = `overlay_dashboard` / `data_viz`\n  - level = `level_1`\n  - template ≈ `examples/ue5-overlay/ue5_overlay_data_viz.html`\n- **生成回放（以样例模板为代表）**：\n  - 实际壳：`ue5-overlay-root + ue5-overlay-viewport + ue5-overlay-safe-area + topbar-hud`，HUD 中包含少量 KPI 与操作，视口中有 world-marker，但没有完整 `alert-center` 面板。\n  - 与预期一致：典型 Mode 1 单 HUD 监控大屏。\n- **审计结果**：\n  - `skill-audit.js ... --scene ue5_overlay --task ue5_overlay_mode_1_single_hud` → `✓ PASS`，`exit=0`。\n\n### 2.2 minimal overlay — 极简无 HUD\n- **任务描述**：\n  - “仅需在三维场景上标注关键工位簇的极简视图，不需要 HUD、不需要告警中心，只保留 world-marker 与基础说明。”\n- **预期路径**：\n  - scene = `ue5_overlay`\n  - task_id = `ue5_overlay_minimal_no_hud`\n  - layout_mode / target_mode = `"minimal"`\n  - page_type = `overlay_dashboard` / `minimal_overlay`\n  - level = `level_1`\n  - template ≈ `examples/ue5-overlay/ue5_overlay_minimal_no_hud.html`\n- **生成回放（以样例模板为代表）**：\n  - 实际壳：`ue5-overlay-root + ue5-overlay-viewport + ue5-overlay-safe-area + world-marker`，没有 `topbar-hud`、`alert-center`、`detail-panel` 或 Dock。\n  - 与预期一致：是真正的“极简无 HUD Overlay”，不再落入 Mode 1 的 HUD Guard。\n- **审计结果**：\n  - `skill-audit.js ... --scene ue5_overlay --task ue5_overlay_minimal_no_hud` → `✓ PASS`，`ERROR: 0 / WARN: 0 / exit=0`。\n\n### 2.3 Mode 2 — HUD + 右侧详情面板\n- **任务描述**：\n  - “质量追踪 Overlay，需要顶部质量 KPI、右侧设备/批次详情面板，下方三维场景 + world-marker，用于追踪质检结果与异常分布。”\n- **预期路径**：\n  - scene = `ue5_overlay`\n  - task_id = `ue5_overlay_mode_2_hud_sidepanel`\n  - layout_mode / target_mode = `2`\n  - page_type = `overlay_dashboard`\n  - level = `level_1`\n  - template ≈ `examples/ue5-overlay/ue5_overlay_quality_tracking.html`\n- **生成回放（以样例模板为代表）**：\n  - 实际壳：`ue5-overlay-root + ue5-overlay-viewport + ue5-overlay-safe-area + topbar-hud + detail-panel`，右侧详情面板承载设备/批次信息和质检记录，不含 Dock。\n  - 与预期一致：典型 Mode 2 HUD + 右侧详情。\n- **审计结果**：\n  - `skill-audit.js ... --scene ue5_overlay --task ue5_overlay_mode_2_hud_sidepanel` → `✓ PASS`，`exit=0`。\n\n### 2.4 Mode 3 — HUD + 告警中心\n- **任务描述**：\n  - “态势感知 Overlay，需要顶部态势 KPI、中部三维场景、左侧或右侧完整告警中心面板，告警是主导任务流，详情与 Dock 不是必需。”\n- **预期路径**：\n  - scene = `ue5_overlay`\n  - task_id = `ue5_overlay_mode_3_hud_alert_center`\n  - layout_mode / target_mode = `3`\n  - page_type = `overlay_dashboard`\n  - level = `level_1`\n  - template ≈ `examples/ue5-overlay/ue5_overlay_dashboard.html`\n- **生成回放（以样例模板为代表）**：\n  - 实际壳：`ue5-overlay-root + ue5-overlay-viewport + ue5-overlay-safe-area + topbar-hud + alert-center`，告警中心面板作为主要列表/队列区域存在，没有 Dock 或双侧固定面板。\n  - 与预期一致：典型 Mode 3 HUD + 告警中心，而非 Mode 5 或驾驶舱。\n- **审计结果**：\n  - `skill-audit.js ... --scene ue5_overlay --task ue5_overlay_mode_3_hud_alert_center` → `✓ PASS`，`exit=0`。\n\n> **ue5_overlay 小结**：针对 Mode 1 / minimal / Mode 2 / Mode 3 四种任务，当前样例在 Guard 与真值源约束下均能稳定体现不同的壳结构，审计一致 PASS。minimal 作为显式任务已不再触发 Mode 1 的 HUD 缺失 ERROR，Mode 2/3 也没有误升到 Dock / 驾驶舱壳。\n\n---\n\n## 3. 回放层面的阶段结论\n\n- **真实生成层面的稳定性**：\n  - 在 8 条代表性的主线任务上，`skill-audit.js` 全部 PASS，`resource_closure_ok=true`，说明当前主线在 HTML 生成约束与资源引用上是稳定的。\n- **结构分化能力**：\n  - `b_system` 中，list / advanced_list / detail / dashboard 分别落在 4 个不同模板壳上，结构差异清晰；`ue5_overlay` 中，Mode 1 / minimal / Mode 2 / Mode 3 也对应 4 套可区分的壳。\n  - 从回放结果看，Phase 1 主线已经能在生成层面体现出“按任务类型切换壳”的分化能力，而不是统一压回单模板。\n- **模板收敛残留**：\n  - b_system 仍然以 `b-system-complete.html` 作为 dashboard canonical 壳，对“生产运营总览”类任务仍有一定收敛，这是当前主线刻意保留的 canonical，而非误收敛。\n  - 在 list / advanced_list / detail 这三类常见任务上，回放未再出现“全部回到 b-system-complete 风格”的现象。\n- **边界漂移风险**：\n  - dashboard vs list：已通过 EXTENSION_GUARD 中的 stop&ask 口径和本次对照回放获得初步约束，但仍可能在缺乏清晰需求时需要澄清主任务流。\n  - minimal vs Mode 1、Mode 2 vs Mode 3：在当前样例下边界清晰，但在更复杂 cockpit/驾驶舱诉求下仍需依赖 EXTENSION_GUARD 中的问询机制。\n\n> 综上，Phase 1 主线在“真实生成回放”这一层已经体现出明显的稳定性和结构分化能力；后续进一步削弱模板收敛与增强 stop&ask，将主要针对更模糊的边界与更丰富的真实任务场景。\n